---
phase: 09-v1.2-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/portable.rs
  - src-tauri/src/apple_intelligence.rs
  - src-tauri/src/audio_toolkit/audio/recorder.rs
  - src-tauri/src/audio_toolkit/text.rs
  - src-tauri/src/commands/audio.rs
  - src-tauri/src/llm_client.rs
  - src-tauri/src/managers/history.rs
  - src-tauri/src/managers/model.rs
  - src-tauri/src/managers/transcription.rs
  - src-tauri/src/overlay.rs
  - src-tauri/src/settings.rs
  - src-tauri/src/shortcut/mod.rs
  - src-tauri/src/transcription_coordinator.rs
  - src-tauri/src/lib.rs
  - .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md
  - .planning/phases/08-privacy-local-first-ux/08-UAT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02, AUDIT-03]
gap_closure: false

must_haves:
  truths:
    - "AUDIT-01: cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings exits 0 from repo root"
    - "AUDIT-01: cargo test --manifest-path src-tauri/Cargo.toml --lib settings::tests passes (no regression from settings.rs derive changes)"
    - "AUDIT-02: .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md exists with status: passed frontmatter and populated truths/artifacts/links tables sourced from 07-01-SUMMARY.md"
    - "AUDIT-03: .planning/phases/08-privacy-local-first-ux/08-UAT.md frontmatter reads status: passed; body references closure commits 47688cc, bbe82db, dbf0a26, dd45f4f, 3433f9e, 4750bd2, cea7eb9, 54b9a85, 6d34e18"
    - "Phase 8 closure work (commits from feat/phase-08-local-first-ux) is incorporated into the Phase 9 branch so the UAT update reflects an actually-merged state"
  artifacts:
    - path: ".planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md"
      provides: "Retroactive verification report promoting SHUT-01/02/03 from partial to satisfied"
      contains: "status: passed"
    - path: ".planning/phases/08-privacy-local-first-ux/08-UAT.md"
      provides: "Refreshed UAT frontmatter with status: passed and references to plans 08-08/09/10"
      contains: "status: passed"
    - path: "src-tauri/src/settings.rs"
      provides: "Six #[derive(Default)] enums replacing manual impl Default blocks; two to_* methods take self by value; HashMap entry pattern"
      contains: "#[derive"
    - path: "src-tauri/src/lib.rs"
      provides: "Removed three double-ref expressions (612, 651, 681), one borrowed-expression (232), one immediately-deref (345), one chained replace (393)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "TECH-DEBT entry tracking llm_client.rs ChatCompletionRequest struct refactor (deferred per Option A choice)"
      contains: "TECH-04"
  key_links:
    - from: ".planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md"
      to: "07-01-SUMMARY.md"
      via: "Truth/artifact tables sourced from §Accomplishments + §Files Created/Modified"
      pattern: "26d4755|e46d75c|722a5b2|cc72158"
    - from: ".planning/phases/08-privacy-local-first-ux/08-UAT.md"
      to: "feat/phase-08-local-first-ux commits"
      via: "## Closure section listing 9 closure SHAs"
      pattern: "47688cc|bbe82db|dbf0a26|dd45f4f|3433f9e|4750bd2|cea7eb9|54b9a85|6d34e18"
    - from: "cargo clippy --all-targets -- -D warnings"
      to: "exit code 0"
      via: "Manual edits to 14 source files per error inventory"
      pattern: "error: aborting due to"
---

<objective>
Close the three v1.2 audit gaps (AUDIT-01 clippy, AUDIT-02 07-VERIFICATION.md backfill, AUDIT-03 08-UAT.md refresh) so the v1.2 milestone can archive with a clean evidence trail.

Purpose: `/gsd:audit-milestone v1.2` currently reports tech_debt: 33 clippy errors block the sampling-rate gate, the Phase 7 verification doc is missing, and the Phase 8 UAT still reads `status: diagnosed` despite gap closure landing in plans 08-08/09/10. Re-running the audit after this plan must report `status: passed`.

Output:
- 14 Rust files clippy-clean (no logic changes, only style/idiom fixes)
- New `.planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md` (status: passed)
- Updated `.planning/phases/08-privacy-local-first-ux/08-UAT.md` (status: passed, closure section added)
- One TECH-DEBT entry in REQUIREMENTS.md for the deferred `llm_client.rs` struct refactor
- Phase 8 branch incorporated into Phase 9 branch so the UAT update reflects merged state
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-v1.2-audit-gap-closure/09-RESEARCH.md
@.planning/phases/09-v1.2-audit-gap-closure/09-VALIDATION.md
@.planning/phases/07-macos-clean-shutdown/07-01-SUMMARY.md
@.planning/phases/08-privacy-local-first-ux/08-UAT.md
@.planning/phases/08-privacy-local-first-ux/08-VERIFICATION.md
@~/.claude/get-shit-done/templates/verification-report.md
@CLAUDE.md
</context>

<interfaces>
<!-- Key facts the executor needs. Extracted directly from 09-RESEARCH.md. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

## Phase 7 commits (for 07-VERIFICATION.md)

| Task | SHA | Message |
|------|-----|---------|
| SHUT-01 diagnosis | `26d4755` | `docs(07): capture diagnosis of quit-unexpectedly crash (SHUT-01)` |
| SHUT-02 flush_and_exit | `e46d75c` | `fix(shutdown): flush logs and run cleanup before exit on macOS (SHUT-02)` |
| SHUT-03 simulate_updater_restart | `722a5b2` | `feat(debug): add simulate_updater_restart trigger for SHUT-03 validation` |
| UPSTREAM.md protection | `cc72158` | `docs(upstream): protect lib.rs quit-exit divergence from future syncs (SHUT-02)` |

## Phase 8 closure commits (for 08-UAT.md body)

| Plan | SHA | Commit message |
|------|-----|----------------|
| 08-08 | `47688cc` | `fix(08-08): make ProviderPicker invoke renderRowExtras for every row` |
| 08-08 | `bbe82db` | `fix(08-08): inline Apple Intelligence Alert, fix Ollama link visibility, hide API key for Custom (local)` |
| 08-08 | `dbf0a26` | `docs(08-08): complete post-processing UI gap-closure plan` |
| 08-09 | `dd45f4f` | `feat(08-09): replace cloud toggle with Local/Cloud tabs in ProviderPicker` |
| 08-09 | `3433f9e` | `refactor(08-09): hoist library to top, remove pillars, wire tab state in PostProcessingSettings` |
| 08-09 | `4750bd2` | `chore(08-09): update en/translation.json — add tabs.*, drop pillars.* + cloudToggle.*, update cloudSelectedNotice` |
| 08-09 | `cea7eb9` | `docs(08-09): complete local-cloud tabs restructure + library hoist + pillars removal` |
| 08-10 | `54b9a85` | `chore(08-10): propagate i18n key changes to 19 sibling locales` |
| 08-10 | `6d34e18` | `docs(08-10): complete sibling-locale i18n propagation plan` |

## Clippy error inventory (14 files, 33 errors)

| File | Line(s) | Error | Fix Strategy |
|------|---------|-------|-------------|
| `src/portable.rs` | 9 | empty line after doc comment | Remove blank line between `///` and `static` |
| `src/portable.rs` | 162 | `write!()` with format string ending in newline | Use `writeln!()` instead |
| `src/apple_intelligence.rs` | 63 | calling `CStr::new` with a byte string literal | Change `CStr::from_bytes_with_nul(b"Unknown error\0").unwrap()` to `c"Unknown error"` |
| `src/audio_toolkit/audio/recorder.rs` | 190 | this can be `std::io::Error::other(_)` | Change `Error::new(ErrorKind::Other, ...)` to `Error::other(...)` |
| `src/audio_toolkit/audio/recorder.rs` | 353 | items after a test module | Move `run_consumer` function (lines 395–402) to before the `mod tests` block |
| `src/audio_toolkit/audio/recorder.rs` | 444 | loop could be `while let` | Rewrite `loop { let chunk = match ... { Ok(c) => c, Err(_) => break, ...} }` as `while let Ok(chunk) = sample_rx.recv()` |
| `src/audio_toolkit/text.rs` | 162 | `map_or` can be simplified | `.map_or(false, |x| x == y)` → `.is_some_and(|x| x == y)` |
| `src/commands/audio.rs` | 24 | `map_or` can be simplified | Same pattern as above |
| `src/llm_client.rs` | 137 | function has too many arguments (8/7) | Add `#[allow(clippy::too_many_arguments)]` (Option A per RESEARCH §Open Q2) |
| `src/managers/history.rs` | 336, 341, 345 | unneeded `return` statement (×3) | Remove `return` keyword from each tail expression |
| `src/managers/model.rs` | 657 | borrowed expression implements required traits | Remove unnecessary `&` borrow |
| `src/managers/transcription.rs` | 119 | `map_or` can be simplified | Same pattern as above |
| `src/overlay.rs` | 310 | `let` binding has unit value | Remove or rewrite the unit binding |
| `src/settings.rs` | 182, 198, 204, 271, 285, 302 | `impl` can be derived (×6) | Add `#[derive(Default)]` + delete manual `impl Default` blocks |
| `src/settings.rs` | 251, 255 | `to_*` method with `&self` should take `self` by value (Copy type) (×2) | Change `&self` to `self` in those two `to_*` methods |
| `src/settings.rs` | 863 | `contains_key` followed by `insert` on HashMap | Rewrite as `.entry(key).or_insert(value)` |
| `src/shortcut/mod.rs` | 286 | `if` statement can be collapsed | Merge nested `if` into single `if a && b` |
| `src/transcription_coordinator.rs` | 65 | `map_or` can be simplified | Same pattern |
| `src/transcription_coordinator.rs` | 167 | `map_or` can be simplified | Same pattern |
| `src/lib.rs` | 232 | borrowed expression implements required traits | Remove unnecessary `&` |
| `src/lib.rs` | 345 | expression creates reference immediately dereferenced | Remove double-ref |
| `src/lib.rs` | 393 | used consecutive `str::replace` calls | Chain as `.replace(['.', '-'], "_")` |
| `src/lib.rs` | 612, 651, 681 | expression creates reference immediately dereferenced (×3) | Remove double-ref at each site |

Total: 33 errors across 14 files.

## Phase 8 branch dependency (critical — see 09-RESEARCH.md Pitfall 5)

`feat/phase-08-local-first-ux` is NOT merged into `feat/phase-09-v1.2-audit-gap-closure` or `main`. The Phase 8 closure commits (listed above) live only on that branch. Without merging Phase 8 first, the AUDIT-03 update to `08-UAT.md` will diverge from Phase 8 and create a merge conflict when both branches are eventually integrated.

Resolution: Task 1 merges `feat/phase-08-local-first-ux` into the current branch BEFORE the AUDIT-03 task runs.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Merge feat/phase-08-local-first-ux into Phase 9 branch (branch incorporation prerequisite)</name>
  <files>(merge commit only — touches whatever files Phase 8 modified, no manual edits)</files>
  <read_first>
    - .planning/phases/09-v1.2-audit-gap-closure/09-RESEARCH.md (Pitfall 5 explains why this must happen first)
    - .planning/phases/08-privacy-local-first-ux/08-VERIFICATION.md (confirms all 6 gaps closed on Phase 8 branch)
  </read_first>
  <action>
This is a prerequisite step required by 09-RESEARCH.md Pitfall 5. The `feat/phase-08-local-first-ux` branch contains 9 closure commits (47688cc, bbe82db, dbf0a26, dd45f4f, 3433f9e, 4750bd2, cea7eb9, 54b9a85, 6d34e18) that Task 4 will reference in `08-UAT.md`. Without merging first, the UAT update will diverge from Phase 8's actual state and create merge conflicts later.

Execute these exact commands from the repo root:

```bash
# 1. Confirm we are on the Phase 9 branch
git -C /Users/pierreviviere/dev/dictus-desktop branch --show-current
# Expected output: feat/phase-09-v1.2-audit-gap-closure

# 2. Confirm Phase 8 branch is NOT yet merged
git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor feat/phase-08-local-first-ux HEAD && echo "ALREADY_MERGED" || echo "NEEDS_MERGE"
# Expected output: NEEDS_MERGE

# 3. Merge Phase 8 into Phase 9 (no-ff to keep history clear)
git -C /Users/pierreviviere/dev/dictus-desktop merge --no-ff feat/phase-08-local-first-ux -m "merge(09): incorporate Phase 8 local-first UX closure commits (08-08/09/10) into Phase 9 branch before AUDIT-03 UAT refresh"

# 4. Verify the 9 closure commits are now ancestors of HEAD
for sha in 47688cc bbe82db dbf0a26 dd45f4f 3433f9e 4750bd2 cea7eb9 54b9a85 6d34e18; do
  git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor "$sha" HEAD && echo "$sha: MERGED" || echo "$sha: MISSING"
done
# Expected: all 9 print "MERGED"

# 5. Confirm `bun run build` and `cargo test --lib settings::tests` still pass after the merge (sanity check before clippy work)
bun run build 2>&1 | tail -5
cargo test --manifest-path /Users/pierreviviere/dev/dictus-desktop/src-tauri/Cargo.toml --lib settings::tests 2>&1 | tail -5
```

If `git merge` reports conflicts, STOP and surface the conflict to the user. Do NOT attempt automated conflict resolution. The two branches diverged on independent surfaces (Phase 8 = i18n/UI, Phase 9 branch = only adds `.planning/phases/09-*` docs + `.gitignore` MCP entry), so conflicts are not expected — but if they appear, they need human judgment.

Do NOT commit anything else in this task. The merge commit produced by step 3 is the entire output.
  </action>
  <verify>
    <automated>for sha in 47688cc bbe82db dbf0a26 dd45f4f 3433f9e 4750bd2 cea7eb9 54b9a85 6d34e18; do git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor "$sha" HEAD || exit 1; done; echo "all-merged"</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor feat/phase-08-local-first-ux HEAD` exits 0
    - `git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor 47688cc HEAD` exits 0
    - `git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor bbe82db HEAD` exits 0
    - `git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor dbf0a26 HEAD` exits 0
    - `git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor dd45f4f HEAD` exits 0
    - `git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor 3433f9e HEAD` exits 0
    - `git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor 4750bd2 HEAD` exits 0
    - `git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor cea7eb9 HEAD` exits 0
    - `git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor 54b9a85 HEAD` exits 0
    - `git -C /Users/pierreviviere/dev/dictus-desktop merge-base --is-ancestor 6d34e18 HEAD` exits 0
    - `git -C /Users/pierreviviere/dev/dictus-desktop log -1 --format=%s` contains `merge(09): incorporate Phase 8`
    - `bun run build` exits 0 (or surfaces only the pre-existing DictusLogo lint, which 08-VERIFICATION.md documents as out-of-scope)
    - `cargo test --manifest-path /Users/pierreviviere/dev/dictus-desktop/src-tauri/Cargo.toml --lib settings::tests` exits 0 with 7/7 passed
  </acceptance_criteria>
  <done>Phase 8 closure commits are ancestors of HEAD; build + settings tests pass on the merged tree; the Phase 9 branch now contains all UI/i18n state that Task 4's UAT refresh will reference.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply 33 clippy fixes across 14 Rust source files + TECH-DEBT note for deferred llm_client refactor (AUDIT-01)</name>
  <files>
    src-tauri/src/portable.rs
    src-tauri/src/apple_intelligence.rs
    src-tauri/src/audio_toolkit/audio/recorder.rs
    src-tauri/src/audio_toolkit/text.rs
    src-tauri/src/commands/audio.rs
    src-tauri/src/llm_client.rs
    src-tauri/src/managers/history.rs
    src-tauri/src/managers/model.rs
    src-tauri/src/managers/transcription.rs
    src-tauri/src/overlay.rs
    src-tauri/src/settings.rs
    src-tauri/src/shortcut/mod.rs
    src-tauri/src/transcription_coordinator.rs
    src-tauri/src/lib.rs
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/09-v1.2-audit-gap-closure/09-RESEARCH.md (AUDIT-01 §Error Map — single source of truth for every fix; also Pitfalls 1-4)
    - src-tauri/src/settings.rs (the six `impl Default` blocks at lines 182, 198, 204, 271, 285, 302; the two `to_*` methods at 251, 255; the `contains_key`/`insert` pair at 863)
    - src-tauri/src/lib.rs (the six lint sites at 232, 345, 393, 612, 651, 681 — read current code BEFORE editing so the executor sees actual `&&` vs `&` patterns)
    - src-tauri/src/audio_toolkit/audio/recorder.rs (lines 190, 353, 395-402, 444 — must see the `mod tests` block bounds before moving `run_consumer`)
    - src-tauri/src/llm_client.rs (line 137 — `send_chat_completion_with_schema` signature, to confirm Option A is appropriate)
    - .planning/REQUIREMENTS.md (current `## Future Requirements > Tech Debt` section — append-only)
    - CLAUDE.md (Rust style: `cargo fmt` + `cargo clippy` before committing)
  </read_first>
  <action>
Apply ALL 33 clippy fixes in the exact file/line table below. No logic changes — every fix is a style/idiom replacement. Per 09-RESEARCH.md Pitfall 1, `cargo fix` does NOT apply these lints automatically; each requires a manual edit. Per 09-RESEARCH.md §Open Question 2, choose **Option A** for `llm_client.rs:137` (`#[allow(clippy::too_many_arguments)]`) — the proper struct refactor is recorded as TECH-DEBT in this task.

**Recommended order (highest density first, per 09-RESEARCH.md):**

### Step 1 — `src-tauri/src/settings.rs` (9 errors, highest density)

1. **Lines 182, 198, 204, 271, 285, 302** — Six `impl Default` blocks that can be derived. For each: add `#[derive(Default)]` to the enum/struct declaration (preserving any existing derives like `Debug, Clone, PartialEq, Serialize, Deserialize`), then delete the entire manual `impl Default for <Type> { fn default() -> Self { <existing variant> } }` block. The default variant must match what the existing manual `impl` returns — to make this explicit on enums, add `#[default]` to that variant inside the enum body. Per 09-RESEARCH.md Pitfall 2, verify after each replacement that the default value is byte-identical to what the manual impl produced (run `cargo test --lib settings::tests` after this step before moving on; existing test `default_enable_cloud_providers_is_false` already guards one such case).

2. **Lines 251, 255** — Two `to_*` methods take `&self` but the type is `Copy`. Change `pub fn to_<x>(&self) -> ...` to `pub fn to_<x>(self) -> ...` for both. Per 09-RESEARCH.md Pitfall 3, since `self` is `Copy`, all existing call sites continue to work without modification — but quickly grep call sites (`grep -rn "to_<method_name>" src-tauri/src/`) to confirm no caller depends on the borrow form.

3. **Line 863** — `if !map.contains_key(&key) { map.insert(key, value); }` → `map.entry(key).or_insert(value);`. The exact rewrite depends on the original code shape; clippy's `help:` suggestion is the source of truth.

### Step 2 — `src-tauri/src/lib.rs` (6 errors)

1. **Line 232** — `borrowed expression implements required traits`. Remove the unnecessary `&` on the borrowed expression. Read the actual line first to see what is being borrowed.

2. **Line 345** — `expression creates a reference which is immediately dereferenced`. Remove the double-ref (e.g., `&*foo` → `foo`).

3. **Line 393** — Two consecutive `.replace()` calls on `'.'` and `'-'`. Chain as a single `.replace(['.', '-'], "_")` call (the slice form of `Pattern`).

4. **Lines 612, 651, 681** — Three identical `expression creates a reference which is immediately dereferenced` sites. Remove the double-ref at each. These are the three sites called out in REQUIREMENTS.md AUDIT-01.

### Step 3 — `src-tauri/src/managers/history.rs` (3 errors)

Lines 336, 341, 345 — three unneeded `return` statements on tail expressions. Remove the `return` keyword and the trailing semicolon (so the expression becomes the function's tail return value).

### Step 4 — `src-tauri/src/audio_toolkit/audio/recorder.rs` (3 errors)

1. **Line 190** — `Error::new(ErrorKind::Other, ...)` → `Error::other(...)`.

2. **Line 353 / function block 395-402** — `items_after_test_module`. The `run_consumer` function (lines 395-402) appears AFTER the `mod tests` block that starts at line 353. Cut the entire `run_consumer` function definition from its current location and paste it BEFORE the `mod tests { ... }` block (e.g., immediately before line 353). Per 09-RESEARCH.md Pitfall 4, function order doesn't affect call resolution in Rust modules, but verify after the move that `run_consumer` is still reachable from its callers (it is — file-level private functions).

3. **Line 444** — `loop { let chunk = match sample_rx.recv() { Ok(c) => c, Err(_) => break, ... } ... }` → `while let Ok(chunk) = sample_rx.recv() { ... }`. Preserve all inner logic; only the loop-control structure changes.

### Step 5 — Files with one error each

- **`src/portable.rs:9`** — Remove the blank line between the `///` doc comment and the `static` item that follows it.
- **`src/portable.rs:162`** — `write!(..., "...\n")` → `writeln!(..., "...")` (drop the trailing `\n` from the format string when switching).
- **`src/apple_intelligence.rs:63`** — `CStr::from_bytes_with_nul(b"Unknown error\0").unwrap()` → `c"Unknown error"` (C-string literal — Rust 1.77+).
- **`src/audio_toolkit/text.rs:162`** — `.map_or(false, |x| <pred>)` → `.is_some_and(|x| <pred>)`.
- **`src/commands/audio.rs:24`** — Same `map_or` → `is_some_and` pattern.
- **`src/managers/model.rs:657`** — `borrowed expression implements required traits`. Remove the unnecessary `&` on the borrowed expression.
- **`src/managers/transcription.rs:119`** — Same `map_or` → `is_some_and` pattern.
- **`src/overlay.rs:310`** — `let _ = <unit-value-expression>;` or `let x = <unit>;` where the binding's value is `()`. Remove the binding (call the expression for its side effect directly: just `<expression>;`) or rewrite per clippy's `help:` suggestion.
- **`src/shortcut/mod.rs:286`** — Two nested `if`s collapsible into `if a && b { ... }`. Read the current shape to see which two conditions to combine.
- **`src/transcription_coordinator.rs:65, 167`** — Both `map_or` → `is_some_and`.

### Step 6 — `src-tauri/src/llm_client.rs:137` (Option A per 09-RESEARCH.md §Open Question 2)

Add `#[allow(clippy::too_many_arguments)]` as an attribute immediately above the `send_chat_completion_with_schema` function declaration at line 137. Do NOT refactor the signature. The proper `ChatCompletionRequest` struct refactor is captured as TECH-DEBT in Step 8 below.

### Step 7 — Run formatter + clippy + tests

```bash
cd /Users/pierreviviere/dev/dictus-desktop
cargo fmt --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml --lib settings::tests
```

All three must exit 0. If clippy still reports errors, address each remaining one in the same style (read the `help:` suggestion, apply, re-run). Do NOT use `#[allow(...)]` to suppress any lint OTHER than the explicitly-approved `too_many_arguments` on `llm_client.rs:137`.

### Step 8 — Append TECH-DEBT entry to REQUIREMENTS.md

In `.planning/REQUIREMENTS.md`, locate the `### Tech Debt` subsection under `## Future Requirements (deferred beyond v1.2)`. Append (preserve existing entries TECH-01, TECH-03, DATA-01):

```markdown
- **TECH-04**: `src-tauri/src/llm_client.rs:137` — refactor `send_chat_completion_with_schema` to take a `ChatCompletionRequest` struct instead of 8 positional arguments. Currently suppressed via `#[allow(clippy::too_many_arguments)]` (added in Phase 9 AUDIT-01 closure 2026-05-28 per 09-RESEARCH.md §Open Question 2 Option A). Refactor risk: API signature change affects all internal callers; estimate 15-30 min once a clean window opens (post-v1.2).
```

Do NOT renumber existing TECH-* IDs. Do NOT modify the BRAND/ICON/SYNC/SHUT/PRIV/AUDIT sections.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus-desktop && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings 2>&1 | tail -5 && cargo test --manifest-path src-tauri/Cargo.toml --lib settings::tests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd /Users/pierreviviere/dev/dictus-desktop && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` exits 0
    - `cd /Users/pierreviviere/dev/dictus-desktop && cargo test --manifest-path src-tauri/Cargo.toml --lib settings::tests` exits 0 with 7/7 passing (including `default_enable_cloud_providers_is_false`)
    - `cd /Users/pierreviviere/dev/dictus-desktop && cargo fmt --manifest-path src-tauri/Cargo.toml --check` exits 0 (no formatting diffs)
    - `grep -c "impl Default for" src-tauri/src/settings.rs` returns a number at least 6 LESS than before the edit (the six manual impls were deleted)
    - `grep -c "#\[derive(.*Default" src-tauri/src/settings.rs` returns at least 6
    - `grep -c "#\[allow(clippy::too_many_arguments)\]" src-tauri/src/llm_client.rs` returns exactly 1
    - `grep -q "TECH-04" .planning/REQUIREMENTS.md` (the new tech-debt entry exists)
    - `grep -q "send_chat_completion_with_schema" .planning/REQUIREMENTS.md` (TECH-04 references the suppressed function)
    - `grep -q "fn run_consumer" src-tauri/src/audio_toolkit/audio/recorder.rs` returns the function (still present)
    - File-order check: the line number of `fn run_consumer` is LESS than the line number of `mod tests {` in `src-tauri/src/audio_toolkit/audio/recorder.rs` (function moved before test module). Run: `awk '/^fn run_consumer/{f=NR} /^mod tests/{t=NR} END{print "fn:" f " mod:" t; exit (f<t)?0:1}' src-tauri/src/audio_toolkit/audio/recorder.rs` exits 0
    - `grep -q "writeln!" src-tauri/src/portable.rs` returns at least 1 hit at the modified location
    - `grep -q 'c"Unknown error"' src-tauri/src/apple_intelligence.rs` returns 1 hit
    - `grep -q "Error::other" src-tauri/src/audio_toolkit/audio/recorder.rs` returns 1 hit
    - `grep -q "while let Ok(chunk) = sample_rx.recv()" src-tauri/src/audio_toolkit/audio/recorder.rs` returns 1 hit
    - `grep -c "is_some_and" src-tauri/src/` (recursive) returns at least 5 (text.rs:162, commands/audio.rs:24, managers/transcription.rs:119, transcription_coordinator.rs:65, transcription_coordinator.rs:167)
  </acceptance_criteria>
  <done>All 33 clippy errors are resolved; `cargo clippy --all-targets -- -D warnings` exits 0; settings tests still pass (no derive-induced regression); `cargo fmt --check` clean; TECH-04 recorded in REQUIREMENTS.md. Commit message: `fix(09): resolve 33 cargo clippy errors across 14 files + record TECH-04 for deferred llm_client refactor (AUDIT-01)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author retroactive 07-VERIFICATION.md from 07-01-SUMMARY.md (AUDIT-02)</name>
  <files>.planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/07-macos-clean-shutdown/07-01-SUMMARY.md (the ONLY source of truth for truths/artifacts/decisions/commits — all content comes from this file)
    - .planning/phases/08-privacy-local-first-ux/08-VERIFICATION.md (living exemplar — copy table shape, frontmatter style, Requirements Coverage section format)
    - ~/.claude/get-shit-done/templates/verification-report.md (schema reference)
    - .planning/REQUIREMENTS.md (lines 41-43 — SHUT-01/02/03 descriptions to mirror in the Requirements Coverage table)
  </read_first>
  <action>
The file `.planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md` does NOT exist. Create it using the Write tool (per 09-RESEARCH.md Pitfall 6 — use Write, not Edit). This is a retroactive verification report: Phase 7 closed on 2026-04-23 without one being authored, and the v1.2 audit flagged this as AUDIT-02. The content source is `07-01-SUMMARY.md` (already complete and self-contained); the structural exemplar is `08-VERIFICATION.md` (the most recent living `passed` report in this repo).

This is an INITIAL pass (`re_verification: false` style — omit the `re_verification` block from frontmatter). The phase has 3 requirements (SHUT-01/02/03) all addressed by a single plan (07-01); the truths table has 3 rows.

Write the file with EXACTLY this content (substitute nothing — the SHAs, line numbers, and dates are all verified from 07-01-SUMMARY.md):

```markdown
---
phase: 07-macos-clean-shutdown
verified: 2026-04-23T00:00:00Z
status: passed
score: 3/3 must-haves verified
---

# Phase 7: macOS Clean Shutdown Verification Report

**Phase Goal:** Quitting Dictus on macOS (via tray menu or post-update relaunch) no longer triggers the OS "quit unexpectedly" crash dialog — root cause is diagnosed before a fix is committed.

**Verified:** 2026-04-23T00:00:00Z (retroactive — authored 2026-05-28 from `07-01-SUMMARY.md` as part of Phase 9 AUDIT-02 closure)
**Status:** passed

**Source of evidence:** `07-01-SUMMARY.md` (the comprehensive single-plan summary closed on 2026-04-23), Phase 7 commits on the merged feature branch, and multi-day real-world validation by Pierre Viviere (no crash dialog observed over several days of daily use).

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SHUT-01: `## Diagnosis` section committed to `07-01-PLAN.md` BEFORE any code change — identifies Suspect A (`tauri-plugin-global-shortcut` Drop releasing CGEventTap from `__cxa_finalize_ranges` on main thread during atexit), rules out Suspects B + C with frame and log evidence | VERIFIED | Commit `26d4755` `docs(07): capture diagnosis of quit-unexpectedly crash (SHUT-01)` — the FIRST commit on the Phase 7 work; `grep -q "## Diagnosis" .planning/phases/07-macos-clean-shutdown/07-01-PLAN.md` passes |
| 2 | SHUT-02: `flush_and_exit(app, code)` helper added to `lib.rs`; both exit sites (tray "Quit Dictus" ~line 255 and no-tray `CloseRequested` branch ~line 623) funnel through it; logs are flushed and CGEventTap is released on macOS via a main-runloop closure before `app.exit()` | VERIFIED | Commit `e46d75c` `fix(shutdown): flush logs and run cleanup before exit on macOS (SHUT-02)`; `grep -n "flush_and_exit" src-tauri/src/lib.rs` finds the definition + 2 call sites |
| 3 | SHUT-03: `simulate_updater_restart` Tauri command registered (calls `AppHandle::restart()` — the same entrypoint `tauri-plugin-updater` invokes); `SimulateUpdaterRestart.tsx` component renders conditionally on `settings.debug_mode`; macOS tray-quit and post-restart flows tested over multi-day window with no "Dictus quit unexpectedly" dialog observed | VERIFIED (with human observation for the no-dialog outcome) | Commit `722a5b2` `feat(debug): add simulate_updater_restart trigger for SHUT-03 validation`; multi-day real-world validation documented in `07-01-SUMMARY.md` §Decisions Made item 4 |

**Score:** 3/3 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src-tauri/src/lib.rs` | `flush_and_exit` helper + both exit sites wired through it + `commands::simulate_updater_restart` in `collect_commands!` | VERIFIED | `grep -n "flush_and_exit" src-tauri/src/lib.rs` shows definition near top + 2 call sites (tray quit ~255, CloseRequested ~623); `simulate_updater_restart` appended to `collect_commands!` |
| `src-tauri/src/commands/mod.rs` | `simulate_updater_restart` Tauri command (calls `AppHandle::restart()`) | VERIFIED | Command present per `07-01-SUMMARY.md` §Files Created/Modified |
| `src/components/settings/debug/SimulateUpdaterRestart.tsx` | New component, `SettingContainer + Button` pattern mirroring `LogDirectory.tsx` | VERIFIED | File listed under `key-files.created` in `07-01-SUMMARY.md` frontmatter |
| `src/components/settings/debug/DebugSettings.tsx` | Conditional render of `SimulateUpdaterRestart` when `settings.debug_mode` is true | VERIFIED | Modification listed in `07-01-SUMMARY.md` §Files Modified |
| `src/bindings.ts` | Auto-regenerated by `tauri-specta` with `simulateUpdaterRestart()` async binding | VERIFIED | Listed under `key-files.modified` (auto-regenerated artifact) |
| `src/i18n/locales/en/translation.json` | `settings.debug.simulateUpdaterRestart.{title,description,button}` keys | VERIFIED | English-only per debug-panel precedent (consistent with the existing `LogDirectory` debug entry) |
| `UPSTREAM.md` | New row in Conflict Rules table flagging `lib.rs` quit-exit / `flush_and_exit` helper as Dictus-wins on future syncs | VERIFIED | Commit `cc72158` `docs(upstream): protect lib.rs quit-exit divergence from future syncs (SHUT-02)` |
| `.planning/phases/07-macos-clean-shutdown/07-01-PLAN.md` | `## Diagnosis` section populated (SHUT-01 gate) | VERIFIED | Commit `26d4755` (the first Phase 7 commit, before any code change) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `app.exit(0)` call sites | log flush + macOS CGEventTap release | `flush_and_exit(app, code)` funnel | WIRED | Both exit sites (tray quit ~255, CloseRequested ~623) in `lib.rs` route through the helper |
| Tray menu "Quit Dictus" handler | `flush_and_exit` | direct call | WIRED | `07-01-SUMMARY.md` §Accomplishments SHUT-02 documents both exit sites funneled through the helper |
| No-tray `CloseRequested` branch | `flush_and_exit` | direct call | WIRED | Same source as above |
| macOS pre-exit cleanup | `global_shortcut().unregister_all()` | `app.run_on_main_thread(...)` closure | WIRED | Releases CGEventTap while runloop is still alive (fix for `__cxa_finalize_ranges` crash path) |
| Logger | `log::logger().flush()` | invoked first in `flush_and_exit` | WIRED | INFO marker logged + buffer flushed before platform cleanup |
| `simulate_updater_restart` Tauri command | `AppHandle::restart()` | command body | WIRED | Same entrypoint as `tauri-plugin-updater` post-update relaunch |
| `SimulateUpdaterRestart.tsx` | `simulate_updater_restart` binding | `simulateUpdaterRestart()` from `src/bindings.ts` | WIRED | tauri-specta auto-binding |
| `SimulateUpdaterRestart.tsx` rendering | `settings.debug_mode` toggle | conditional render in `DebugSettings.tsx` | WIRED | Component hidden unless debug mode enabled |
| Future upstream sync of `lib.rs` | UPSTREAM.md Conflict Rules table | human reviewer reads table during merge | WIRED | New row flags `lib.rs` quit-exit / `flush_and_exit` as Dictus-wins |

---

### Requirements Coverage

| Requirement | Source plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| SHUT-01 | 07-01 | Console.app crash report read and crashing thread identified; diagnosis documented in phase plan BEFORE fix is chosen | SATISFIED | Commit `26d4755` — `## Diagnosis` section in `07-01-PLAN.md` committed before code changes; Suspect A (`tauri-plugin-global-shortcut` Drop) identified, Suspects B/C ruled out |
| SHUT-02 | 07-01 | Path (a) graceful cleanup applied: explicit `global_shortcut().unregister_all()` on main thread via `app_handle.run_on_main_thread` before `app.exit(0)` at both `lib.rs` exit sites | SATISFIED | Commit `e46d75c` — `flush_and_exit` helper introduced; both exit sites routed through it; macOS pre-exit cleanup releases CGEventTap while runloop is alive |
| SHUT-03 | 07-01 | Clean quit on macOS Sequoia 15.x (tray menu "Quit Dictus" + post-auto-update relaunch) no longer triggers the "Dictus quit unexpectedly" OS dialog | SATISFIED | Commit `722a5b2` adds `simulate_updater_restart` debug trigger; Pierre's multi-day real-world validation window (2026-04-16 → 2026-04-23) saw no crash dialog on tray quit or post-restart relaunch |

**Orphan check:** REQUIREMENTS.md maps SHUT-01, SHUT-02, SHUT-03 to Phase 7 only. All three IDs are claimed by Plan 07-01's `requirements-completed` frontmatter. No orphaned requirements.

---

### Anti-Patterns Found

None. The Phase 7 change set is surgical (1 helper + 2 call-site rewires + 1 debug command + 1 UPSTREAM.md row + 1 i18n entry). No `TODO` / `FIXME` / `XXX` / `HACK` / `PLACEHOLDER` markers in the modified files. No stub or unimplemented branches.

---

### Human Verification Required

### 1. macOS "Dictus quit unexpectedly" dialog absence (SHUT-03)

**Test:** On macOS Sequoia 15.x, with a Phase 7 build installed: (a) launch the app, (b) click tray icon → "Quit Dictus", (c) confirm no "Dictus quit unexpectedly — Reopen / Report / Ignore" dialog appears. Repeat after a forced `simulate_updater_restart` from the Debug Settings panel.

**Expected:** No crash dialog under any path. App terminates cleanly. `Console.app` shows no abort/segfault entries for `dictus` around the quit timestamp.

**Why human:** The crash dialog is an OS-level UI surface that only renders on a physical macOS device with a live windowing session. There is no programmatic API to observe it. Validation is qualitative (dialog appears / does not appear) and required multi-day observation in a normal-use environment.

**Result (recorded 2026-04-23):** Pierre Viviere — macOS Sequoia 15.x, Apple Silicon — multi-day daily-use validation window (2026-04-16 through 2026-04-23). No "Dictus quit unexpectedly" dialog observed across tray-quit and post-relaunch flows. Documented in `07-01-SUMMARY.md` §Accomplishments "Real-world validation" and §Decisions Made item 4.

---

### Gaps Summary

**No gaps found.** Phase goal achieved. All three SHUT requirements satisfied; the fix is defensible on first principles (flushing logs + releasing CGEventTap on a live runloop is correct regardless of whether the `atexit`-path was the only original trigger); UPSTREAM.md protection guards the divergence from future upstream syncs.

**Note on non-reproducibility under clean env:** `07-01-SUMMARY.md` §Decisions Made item 4 (and the user's memory note `project_macos_quit_crash_investigation`) flag that the original crash may have been partly triggered by multi-install pollution. The SHUT-02 fix is still correct and defensive; the phase was intentionally closed without a second reproduction because the validation window was sufficient and the fix addresses a real bug class (`atexit`-time CGEventTap release on main thread).

---

## Verification Metadata

**Verification approach:** Goal-backward — derived from the Phase 7 success criteria in ROADMAP.md (3 truths, mapping 1:1 to SHUT-01/02/03)
**Must-haves source:** `07-01-SUMMARY.md` frontmatter (`requirements-completed: [SHUT-01, SHUT-02, SHUT-03]`)
**Automated checks:** 1 grep check for `## Diagnosis` in `07-01-PLAN.md` (passes); 1 grep check for `flush_and_exit` in `lib.rs` (passes — finds definition + 2 call sites)
**Human checks required:** 1 (macOS "quit unexpectedly" dialog absence — completed 2026-04-23 over multi-day validation window)
**Total verification time:** ~1h coding + multi-day real-world validation window
**Report authored:** 2026-05-28 (retroactive, as part of Phase 9 AUDIT-02 closure)

---

*Verified: 2026-04-23T00:00:00Z (closure date) — retroactive report authored 2026-05-28*
*Verifier: Pierre Viviere (multi-day real-world observation) + Claude (gsd-planner, retroactive doc authoring per 09-RESEARCH.md AUDIT-02)*
*Phase 7 closed: 2026-04-23 — `07-01-SUMMARY.md`*
```

Write the file with the Write tool (Path: `.planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`). Do NOT use Edit (file does not exist yet — per 09-RESEARCH.md Pitfall 6).

After writing, validate the frontmatter:

```bash
node "/Users/pierreviviere/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md --schema verification 2>&1 || echo "Schema check unavailable or non-fatal"
```

If the verification schema is not registered in gsd-tools, fall back to the grep checks in the acceptance criteria below.
  </action>
  <verify>
    <automated>test -f /Users/pierreviviere/dev/dictus-desktop/.planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md && grep -q "^status: passed$" /Users/pierreviviere/dev/dictus-desktop/.planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md && grep -qE "26d4755|e46d75c|722a5b2|cc72158" /Users/pierreviviere/dev/dictus-desktop/.planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f /Users/pierreviviere/dev/dictus-desktop/.planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md` exits 0
    - `grep -q "^phase: 07-macos-clean-shutdown$" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "^verified: 2026-04-23T00:00:00Z$" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "^status: passed$" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "^score: 3/3 must-haves verified$" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "26d4755" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "e46d75c" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "722a5b2" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "cc72158" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "SHUT-01" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "SHUT-02" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "SHUT-03" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "## Goal Achievement" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "### Observable Truths" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "### Required Artifacts" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "### Key Link Verification" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "### Requirements Coverage" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "### Human Verification Required" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "### Gaps Summary" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "flush_and_exit" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
    - `grep -q "simulate_updater_restart" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md`
  </acceptance_criteria>
  <done>`07-VERIFICATION.md` exists with `status: passed`, 3/3 truths populated, all four Phase 7 commit SHAs referenced, structure mirrors `08-VERIFICATION.md` (the living exemplar). Commit message: `docs(09): backfill 07-VERIFICATION.md retroactively from 07-01-SUMMARY (AUDIT-02)`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Refresh 08-UAT.md frontmatter to status: passed and add Closure section with 9 commit SHAs (AUDIT-03)</name>
  <files>.planning/phases/08-privacy-local-first-ux/08-UAT.md</files>
  <read_first>
    - .planning/phases/08-privacy-local-first-ux/08-UAT.md (current state — must see existing body to know what to preserve vs. what to add)
    - .planning/phases/08-privacy-local-first-ux/08-VERIFICATION.md (the re-verification report — has the canonical gap-closure mapping with the exact SHAs and gap descriptions to mirror in the new Closure section)
    - .planning/phases/09-v1.2-audit-gap-closure/09-RESEARCH.md (AUDIT-03 §Target State block — gives the exact frontmatter to write)
  </read_first>
  <action>
This task depends on Task 1 — `feat/phase-08-local-first-ux` must already be merged into the current branch so the file on disk matches the state the new frontmatter describes. Per 09-RESEARCH.md Pitfall 5, do not run this task before Task 1 completes.

The body content of `08-UAT.md` (Tests 1-14, Summary, Gaps section) is already accurate and should NOT be rewritten. The only changes are:

1. **Replace the entire YAML frontmatter block** (lines 1-17 of the current file) with the new target frontmatter shown below.
2. **Append a new `## Closure` section** at the END of the file (after the existing `## Gaps` section closes, after `debug_session: ""`).

Do NOT touch the `## Current Test`, `## Tests`, `## Summary`, or `## Gaps` sections — they document the historical UAT run and must be preserved verbatim.

### Step 1 — Replace frontmatter

**Current frontmatter (lines 1-17 — DELETE THIS BLOCK):**
```yaml
---
status: diagnosed
phase: 08-privacy-local-first-ux
source:
  - 08-01-SUMMARY.md
  - 08-02-SUMMARY.md
  - 08-03-SUMMARY.md
  - 08-04-SUMMARY.md
started: 2026-05-22T00:00:00Z
updated: 2026-05-22T00:00:00Z
diagnosis_note: |
  Root cause is a design pivot rejected by user, not a code bug — skipped
  parallel debug agents (no investigation needed). Closure split per user
  decision (2026-05-22): UI-only items handled as a Phase 8 sub-phase
  (gap-closure plan); embedded LLM runtime items deferred to a new
  milestone "Local-First Models" promoted ahead of v1.3 Smart Modes.
---
```

**New frontmatter (WRITE THIS EXACT BLOCK):**
```yaml
---
status: passed
phase: 08-privacy-local-first-ux
source:
  - 08-01-SUMMARY.md
  - 08-02-SUMMARY.md
  - 08-03-SUMMARY.md
  - 08-04-SUMMARY.md
  - 08-08-SUMMARY.md
  - 08-09-SUMMARY.md
  - 08-10-SUMMARY.md
started: 2026-05-22T00:00:00Z
updated: 2026-05-28T00:00:00Z
closure_note: |
  All 6 gaps from the 2026-05-22 partial-pass UAT (3 bugs + 3 design pivots)
  closed in source on feat/phase-08-local-first-ux via plans 08-08, 08-09,
  08-10. Re-verification (08-VERIFICATION.md, 2026-05-22T21:00:00Z) reports
  11/11 truths verified, 0 gaps remaining, 0 regressions. Closure commits
  referenced in ## Closure section below.
---
```

Note: the `diagnosis_note` field is REMOVED; the new `closure_note` field replaces it. The `source` list gains three entries (08-08/09/10 SUMMARYs). The `updated` field bumps to 2026-05-28. The `status` field changes from `diagnosed` to `passed`.

### Step 2 — Append `## Closure` section

Append the following block at the END of the file (after the existing `debug_session: ""` line that closes the `## Gaps` YAML literal). Add ONE blank line between `debug_session: ""` and the new `## Closure` header:

```markdown

## Closure

All 6 gaps from the 2026-05-22 partial-pass UAT (test 9 issue + 5 skipped follow-ups) are closed in source on `feat/phase-08-local-first-ux` and verified by `08-VERIFICATION.md` (re-verification pass, 11/11 truths, 0 gaps remaining). The Phase 8 branch was merged into the Phase 9 branch on 2026-05-28 as part of AUDIT-03 closure.

### Gap → closure plan → commit mapping

| Gap | Description | Plan | Commit(s) |
| --- | ----------- | ---- | --------- |
| 1a  | Apple Intelligence unavailability Alert inline within `apple_intelligence` provider card | 08-08 | `47688cc` (renderRowExtras invoked for every row), `bbe82db` (Alert branched on `option.value === "apple_intelligence"`) |
| 2a  | Ollama link visually identifiable as a link at rest (underline) | 08-08 | `bbe82db` (className `underline underline-offset-2 hover:opacity-80`) |
| 2b  | API key field hidden when "Custom (local)" is the selected provider | 08-08 | `bbe82db` (`state.selectedProvider?.id !== "custom"` gate) |
| 5   | Provider area uses Local / Cloud tabs (not the rejected cloud opt-in toggle) | 08-09 + 08-10 | `dd45f4f` (tabs control), `3433f9e` (tab-state wiring in parent), `4750bd2` (EN i18n), `54b9a85` (19 sibling locales) |
| 6   | Three-pillar marketing block (Confidentialité / Contrôle / Expérience) removed from page | 08-09 + 08-10 | `3433f9e` (JSX delete), `4750bd2` (EN keys delete), `54b9a85` (locale propagation) |
| 7   | Library coming-soon teaser hoisted to top of post-processing page | 08-09 | `3433f9e` (JSX reordered to first child after page header) |

### All closure commits (full SHA list)

- `47688cc` — `fix(08-08): make ProviderPicker invoke renderRowExtras for every row`
- `bbe82db` — `fix(08-08): inline Apple Intelligence Alert, fix Ollama link visibility, hide API key for Custom (local)`
- `dbf0a26` — `docs(08-08): complete post-processing UI gap-closure plan`
- `dd45f4f` — `feat(08-09): replace cloud toggle with Local/Cloud tabs in ProviderPicker`
- `3433f9e` — `refactor(08-09): hoist library to top, remove pillars, wire tab state in PostProcessingSettings`
- `4750bd2` — `chore(08-09): update en/translation.json — add tabs.*, drop pillars.* + cloudToggle.*, update cloudSelectedNotice`
- `cea7eb9` — `docs(08-09): complete local-cloud tabs restructure + library hoist + pillars removal`
- `54b9a85` — `chore(08-10): propagate i18n key changes to 19 sibling locales`
- `6d34e18` — `docs(08-10): complete sibling-locale i18n propagation plan`

### Build / test posture (verified 2026-05-22, re-confirmed 2026-05-28 after Phase 8 merged into Phase 9)

- `bun run check:translations` → "All 19 languages have complete translations!" (exit 0)
- `bun run build` → built in 1.95s (exit 0)
- `cargo test --manifest-path src-tauri/Cargo.toml --lib settings::tests` → 7/7 passed (exit 0)

### Re-verification reference

`.planning/phases/08-privacy-local-first-ux/08-VERIFICATION.md` (status: passed, score: 11/11, re_verification block documents all 6 gaps closed and 0 regressions).
```

Do not delete or modify any existing line of the file body (Tests 1-14, Summary, Gaps). Only:
- Replace lines 1-17 (frontmatter)
- Append the `## Closure` section after the existing body ends
  </action>
  <verify>
    <automated>grep -q "^status: passed$" /Users/pierreviviere/dev/dictus-desktop/.planning/phases/08-privacy-local-first-ux/08-UAT.md && grep -q "47688cc" /Users/pierreviviere/dev/dictus-desktop/.planning/phases/08-privacy-local-first-ux/08-UAT.md && grep -q "^## Closure$" /Users/pierreviviere/dev/dictus-desktop/.planning/phases/08-privacy-local-first-ux/08-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "^status: passed$" .planning/phases/08-privacy-local-first-ux/08-UAT.md` (frontmatter status updated)
    - `grep -q "^updated: 2026-05-28T00:00:00Z$" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "closure_note:" .planning/phases/08-privacy-local-first-ux/08-UAT.md` (closure_note field present)
    - `! grep -q "^status: diagnosed$" .planning/phases/08-privacy-local-first-ux/08-UAT.md` (old status removed)
    - `! grep -q "diagnosis_note:" .planning/phases/08-privacy-local-first-ux/08-UAT.md` (old diagnosis_note removed)
    - `grep -q "08-08-SUMMARY.md" .planning/phases/08-privacy-local-first-ux/08-UAT.md` (new source entry)
    - `grep -q "08-09-SUMMARY.md" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "08-10-SUMMARY.md" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "^## Closure$" .planning/phases/08-privacy-local-first-ux/08-UAT.md` (new section header)
    - `grep -q "47688cc" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "bbe82db" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "dbf0a26" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "dd45f4f" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "3433f9e" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "4750bd2" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "cea7eb9" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "54b9a85" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "6d34e18" .planning/phases/08-privacy-local-first-ux/08-UAT.md`
    - `grep -q "## Current Test" .planning/phases/08-privacy-local-first-ux/08-UAT.md` (original body preserved)
    - `grep -q "## Tests" .planning/phases/08-privacy-local-first-ux/08-UAT.md` (original body preserved)
    - `grep -q "## Summary" .planning/phases/08-privacy-local-first-ux/08-UAT.md` (original body preserved)
    - `grep -q "## Gaps" .planning/phases/08-privacy-local-first-ux/08-UAT.md` (original body preserved)
    - `grep -c '### [0-9]' .planning/phases/08-privacy-local-first-ux/08-UAT.md` returns 14 (all original test sections still present)
  </acceptance_criteria>
  <done>`08-UAT.md` frontmatter reads `status: passed`, `updated: 2026-05-28T00:00:00Z`, with `closure_note` and three new SUMMARY refs; new `## Closure` section at end lists all 9 closure SHAs; original Tests/Summary/Gaps body preserved verbatim. Commit message: `docs(09): refresh 08-UAT.md to status:passed + add Closure section with 9 commit SHAs (AUDIT-03)`.</done>
</task>

</tasks>

<verification>

## Plan-level verification (run after all 4 tasks complete)

### Automated phase gate (must all exit 0)

```bash
cd /Users/pierreviviere/dev/dictus-desktop

# AUDIT-01
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml --lib settings::tests
cargo fmt --manifest-path src-tauri/Cargo.toml --check

# AUDIT-02
test -f .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md
grep -q "^status: passed$" .planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md

# AUDIT-03
grep -q "^status: passed$" .planning/phases/08-privacy-local-first-ux/08-UAT.md
grep -qE "47688cc|bbe82db|dbf0a26|dd45f4f|3433f9e|4750bd2|cea7eb9|54b9a85|6d34e18" .planning/phases/08-privacy-local-first-ux/08-UAT.md

# Phase 8 branch incorporation (prerequisite for AUDIT-03)
git merge-base --is-ancestor feat/phase-08-local-first-ux HEAD

# TECH-DEBT carry-over
grep -q "TECH-04" .planning/REQUIREMENTS.md
```

### Manual phase gate (one human-driven check)

Re-run `/gsd:audit-milestone v1.2` and confirm the report reads `status: passed` with no partial requirements and no integration gaps. This is the integration test for all three AUDIT requirements collectively. (Per 09-VALIDATION.md Manual-Only Verifications table.)

</verification>

<success_criteria>

### Measurable completion

- [ ] `feat/phase-08-local-first-ux` is an ancestor of HEAD on `feat/phase-09-v1.2-audit-gap-closure` (Task 1)
- [ ] `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` exits 0 (Task 2 — AUDIT-01)
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml --lib settings::tests` exits 0 with 7/7 passed (Task 2 — no regression from settings.rs derive changes)
- [ ] `cargo fmt --manifest-path src-tauri/Cargo.toml --check` exits 0 (Task 2)
- [ ] `.planning/REQUIREMENTS.md` contains a `TECH-04` entry referencing `send_chat_completion_with_schema` (Task 2)
- [ ] `.planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md` exists with `status: passed`, `score: 3/3`, all 4 Phase 7 commit SHAs referenced, all 7 template sections present (Task 3 — AUDIT-02)
- [ ] `.planning/phases/08-privacy-local-first-ux/08-UAT.md` frontmatter reads `status: passed` + `updated: 2026-05-28T00:00:00Z` + 7 source SUMMARYs (Task 4 — AUDIT-03)
- [ ] `08-UAT.md` body contains a new `## Closure` section listing all 9 Phase 8 closure SHAs (Task 4)
- [ ] Original `08-UAT.md` body (Tests 1-14, Summary, Gaps) preserved verbatim (Task 4 regression guard)
- [ ] Re-running `/gsd:audit-milestone v1.2` reports `status: passed` with no partial requirements (phase-gate integration test)

</success_criteria>

<output>
After all 4 tasks complete and the phase-gate checks pass, create `.planning/phases/09-v1.2-audit-gap-closure/09-01-SUMMARY.md` following the template at `~/.claude/get-shit-done/templates/summary.md`.

The summary should document:
- All 33 clippy fixes applied (file/line breakdown) + the Option A choice for `llm_client.rs` + TECH-04 carry-over
- The retroactive `07-VERIFICATION.md` authored from `07-01-SUMMARY.md` (status: passed, 3/3)
- The `08-UAT.md` frontmatter refresh + Closure section addition (status: passed, 9 SHAs)
- The Phase 8 → Phase 9 branch merge as the AUDIT-03 prerequisite
- Final phase-gate result from `/gsd:audit-milestone v1.2` (the milestone integration test)

Mark `requirements-completed: [AUDIT-01, AUDIT-02, AUDIT-03]` in the SUMMARY frontmatter.
</output>
