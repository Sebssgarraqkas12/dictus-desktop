---
report: v1.2 milestone cross-phase integration check
generated: 2026-05-29
scope: Phases 6, 7, 8, 9
branch: feat/phase-09-v1.2-audit-gap-closure
---

# v1.2 "Polish & Local-First UX" — Integration Check

This report verifies cross-phase wiring and end-to-end (E2E) user flows for the v1.2 milestone after Phase 9's audit-gap-closure work. Existence of files was previously verified at phase level; this check focuses on **connections** between phases.

---

## 1. Cross-Phase Wiring Concerns

### 1.1 Phase 6 verify-sync.sh after Phase 7 + Phase 8 changes — **WIRED**

| Check | Status | Evidence |
|-------|--------|----------|
| `bash .github/scripts/verify-sync.sh` exits 0 | PASS | All 15 checks pass (SYNC-05a..k, BRAND-01a/02a/03a, ICON-02a) |
| No `handy-*.wav` in src-tauri | PASS | BRAND-01a check on `handy_keys`-filtered grep returns no match |
| No `"Handy Portable Mode"` string in `src-tauri/src/portable.rs` | PASS | `portable.rs:29/97/112/161` all read `"Dictus Portable Mode"` |
| No hardcoded `%APPDATA%/handy` in `DebugPaths.tsx` | PASS | `DebugPaths.tsx:23` reads `await commands.getAppDirPath()` |
| ICON-02a layer assertion holds | PASS | `identify -format '%w\n' src-tauri/icons/icon.ico` returns exactly 6 unique widths: 16, 24, 32, 48, 64, 256 |
| `src-tauri/src/lib.rs` (Phase 7) contains no `handy-` regression | PASS | grep returns empty |
| `src-tauri/src/settings.rs` (Phase 8 rewrite + Phase 9 derive substitutions) contains no `handy-` regression | PASS | grep returns empty |
| `PostProcessingSettings.tsx` (Phase 8) contains no `handy-` regression | PASS | grep returns empty |

**Affected REQ-IDs:** BRAND-01, BRAND-02, BRAND-03, ICON-02, SYNC-05, SYNC-06.

### 1.2 Phase 7 `flush_and_exit` survives Phase 8 settings.rs rewrite — **WIRED**

| Check | Status | Evidence |
|-------|--------|----------|
| `flush_and_exit` defined at the expected line | PASS | `src-tauri/src/lib.rs:81` (declared at 81; doc-comment 65-80; body 81-105) |
| Tray "quit" arm funnels through helper | PASS | `lib.rs:296-302` — `"quit" =>` arm calls `flush_and_exit(app, 0)` at line 301 |
| `CloseRequested` no-tray branch funnels through helper | PASS | `lib.rs:669-676` — `else` (no tray) branch calls `flush_and_exit(&app_handle, 0)` at line 675 |
| `log::logger().flush()` present | PASS | `lib.rs:83` (pre-cleanup) and `lib.rs:102` (post-cleanup, after the 50ms yield) |
| `tauri_plugin_global_shortcut` main-thread `unregister_all()` intact | PASS | `lib.rs:86-97` — `use ... GlobalShortcutExt; ... app.run_on_main_thread(move || ... global_shortcut().unregister_all())` |
| `#[cfg(target_os = "macos")]` cleanup gate preserved | PASS | `lib.rs:84` (cleanup block macOS-gated) and `lib.rs:88-103` (`run_on_main_thread` + `unregister_all` + 50ms yield) |

The Phase 8 rewrite of `settings.rs` and Phase 9's six `#[derive(Default)]` substitutions did not touch `lib.rs`'s shutdown surface. **Affected REQ-IDs:** SHUT-01, SHUT-02.

### 1.3 Phase 7 `simulate_updater_restart` Tauri command still registered — **WIRED**

| Check | Status | Evidence |
|-------|--------|----------|
| Command symbol present in `commands/mod.rs` | PASS | `commands/mod.rs:201-206` — `#[tauri::command] #[specta::specta] pub fn simulate_updater_restart(app: AppHandle) { ... app.restart(); }` |
| Command listed in `collect_commands!` macro | PASS | `lib.rs:475` — `commands::simulate_updater_restart,` in the specta builder |
| TypeScript binding generated | PASS | `src/bindings.ts:558-559` — `async simulateUpdaterRestart() : Promise<void> { await TAURI_INVOKE("simulate_updater_restart"); }` |
| Frontend component imports binding | PASS | `SimulateUpdaterRestart.tsx:33` — `await commands.simulateUpdaterRestart()` |
| `DebugSettings.tsx` mounts component | PASS | `DebugSettings.tsx:7` import + `:36` JSX `<SimulateUpdaterRestart descriptionMode="tooltip" grouped={true} />` |
| Gated on `debug_mode` | PASS (by parent gate) | `DebugSettings.tsx` is itself rendered only when `settings.debug_mode === true`; the inline comment at line 34 documents the SHUT-03 wiring |

**Affected REQ-IDs:** SHUT-03.

### 1.4 Phase 8 platform-aware default + Custom (local) relabel intact after Phase 9 clippy sweep — **WIRED**

| Check | Status | Evidence |
|-------|--------|----------|
| cfg-conditional default at expected location | PASS | `settings.rs:496-505` — `default_post_process_provider_id()` returns `APPLE_INTELLIGENCE_PROVIDER_ID` on `macos+aarch64`, `"custom"` otherwise |
| "Custom (local)" label preserved | PASS | `settings.rs:578` — `label: "Custom (local)".to_string()` and `settings.rs:983` test assertion `assert_eq!(custom.label, "Custom (local)")` |
| `default_enable_cloud_providers()` still returns `false` | PASS | `settings.rs:482-484` |
| 7/7 settings unit tests pass | PASS | `cargo test --lib settings` → 7 passed, 0 failed (`default_enable_cloud_providers_is_false`, `default_post_process_provider_id_returns_apple_on_macos_arm64`, `default_post_process_providers_includes_custom_with_stable_id`, `secret_map_debug_redacts_values`, `debug_output_redacts_api_keys`, `default_settings_disable_auto_submit`, `default_settings_have_cloud_providers_disabled`) |
| Full lib test suite green | PASS | 69 passed, 0 failed |

**Affected REQ-IDs:** PRIV-01, PRIV-03.

### 1.5 Phase 9 clippy gate green for whole tree — **WIRED**

| Check | Status | Evidence |
|-------|--------|----------|
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` exits 0 | PASS | Tail output: `Finished 'dev' profile [unoptimized + debuginfo] target(s) in 0.71s` — no warnings/errors emitted. Only informational notices ("unused patches", Apple Intelligence build info, tray translations) which are pre-clippy build messages, not lint findings. |
| TECH-04 deferred refactor recorded | PASS | `.planning/REQUIREMENTS.md:72` — `TECH-04: ... refactor send_chat_completion_with_schema ... currently suppressed via #[allow(clippy::too_many_arguments)]` |

**Affected REQ-IDs:** AUDIT-01.

### 1.6 Phase 8 i18n parity across 19 locales — **WIRED**

| Check | Status | Evidence |
|-------|--------|----------|
| `bun run check:translations` exits 0 | PASS | All 19 locales (AR, BG, CS, DE, ES, FR, HE, IT, JA, KO, PL, PT, RU, SV, TR, UK, VI, ZH, ZH-TW) report "All keys present"; reference EN has 431 keys |
| `tabs.local` / `tabs.cloud` keys added | PASS | `src/i18n/locales/en/translation.json:422-424` — `"tabs": { "local": "Local", "cloud": "Cloud" }` |
| `pillars` removed | PASS | `grep -rn "pillars" src/i18n/locales/` returns no match |
| `cloudToggle` removed | PASS | `grep -rn "cloudToggle" src/i18n/locales/` returns no match |

**Affected REQ-IDs:** PRIV-01.

### 1.7 Phase 9 verification artifacts wired — **WIRED**

| Check | Status | Evidence |
|-------|--------|----------|
| `07-VERIFICATION.md` exists with `status: passed` | PASS | `.planning/phases/07-macos-clean-shutdown/07-VERIFICATION.md:4` — `status: passed`; score `3/3 must-haves verified` |
| `08-UAT.md` frontmatter `status: passed` | PASS | `08-UAT.md:2` — `status: passed` |
| `08-UAT.md` closure SHAs present | PASS | `08-UAT.md:196-208` — all 9 Phase 8 closure SHAs cited (47688cc, bbe82db, dbf0a26, dd45f4f, 3433f9e, 4750bd2, cea7eb9, 54b9a85, 6d34e18); `## Closure` section at line 188 |
| `09-VERIFICATION.md` ratifies both | PASS | `09-VERIFICATION.md` rows 3, 4, AUDIT-02, AUDIT-03 all marked VERIFIED/SATISFIED |

**Affected REQ-IDs:** AUDIT-02, AUDIT-03.

---

## 2. E2E User Flows

### F1 — User records audio → filename starts with `dictus-` — **COMPLETE**

| Step | Status | Evidence |
|------|--------|----------|
| 1. Recording trigger reaches `actions.rs` | OK | `actions.rs:538` uses `format!("dictus-{}.wav", chrono::Utc::now().timestamp())` |
| 2. History manager writes audio | OK | `managers/history.rs:689` uses `format!("dictus-{}.wav", timestamp)` |
| 3. History UI reads from `commands.getAppDirPath()` | OK | `DebugPaths.tsx:23` |
| 4. App icon renders on all platforms | OK | Linux PNGs 32/64/128/256/512 verified opaque (`identify [opaque]=True`); ICO has 6 layers (16, 24, 32, 48, 64, 256); icon.icns present |

**Affected REQ-IDs:** BRAND-01, BRAND-03, ICON-01, ICON-02, ICON-03.

### F2 — User opens Dictus → tray "Quit Dictus" → clean exit — **COMPLETE**

Full code path:

1. **Tray menu builds with id `"quit"`** — `tray.rs:129` `MenuItem::with_id(app, "quit", &strings.quit, ...)`
2. **Menu event matches** — `lib.rs:296-302` `"quit" =>` arm
3. **Helper invoked** — `lib.rs:301` `flush_and_exit(app, 0)`
4. **Log flush** — `lib.rs:83` `log::logger().flush()`
5. **Main-thread global-shortcut unregister (macOS)** — `lib.rs:86-97` `run_on_main_thread(move || ... global_shortcut().unregister_all())`
6. **50 ms yield + second flush** — `lib.rs:101-102`
7. **Exit** — `lib.rs:104` `app.exit(code)`

Symmetric no-tray path: `lib.rs:669-676` (CloseRequested → `flush_and_exit(&app_handle, 0)`). Both call sites named in `flush_and_exit` doc-comment (`lib.rs:65-80`). UPSTREAM.md conflict-rules row protects against regression.

**Affected REQ-IDs:** SHUT-01, SHUT-02.

### F3 — User opens Settings → Post-Processing → sees Local/Cloud tabs — **COMPLETE**

| Step | Status | Evidence |
|------|--------|----------|
| 1. Page parent loads `state.selectedProviderId` from settings | OK | `PostProcessingSettings.tsx:35` `selectedIsCloud = ...`; `:60-61` derives initial tab from `LOCAL_PROVIDER_IDS_SET` |
| 2. Default on macOS arm64 = `apple_intelligence` | OK | `settings.rs:496-500` (cfg) + `settings.rs:565-572` (provider definition) |
| 3. Default elsewhere = `custom` (local) | OK | `settings.rs:501-504` + `settings.rs:576-583` |
| 4. Tabs control rendered | OK | `ProviderPicker.tsx:70-100` with `tabs.local` / `tabs.cloud` triggers |
| 5. Cloud providers behind Cloud tab only | OK | `ProviderPicker.tsx:77` `nextTab === "local" ? lastLocalId : lastCloudId` swap logic |
| 6. "Learn more" link opens PRIVACY.md | OK | `PostProcessingSettings.tsx:590-594` `handleLearnMore()` → `openUrl("https://github.com/getdictus/dictus-desktop/blob/main/docs/PRIVACY.md")`; rendered at `:606-621` |

**Affected REQ-IDs:** PRIV-01, PRIV-02, PRIV-03.

### F4 — User reads README → finds Privacy section linking to docs/PRIVACY.md — **COMPLETE**

| Step | Status | Evidence |
|------|--------|----------|
| 1. README has `## Privacy` heading | OK | `README.md:53` |
| 2. Section links to `docs/PRIVACY.md` | OK | `README.md:55` — `[docs/PRIVACY.md](docs/PRIVACY.md)` |
| 3. Target file exists | OK | `docs/PRIVACY.md` (9815 bytes) |

**Affected REQ-IDs:** PRIV-02.

### F5 — Debug mode → `SimulateUpdaterRestart` button → `AppHandle::restart()` — **COMPLETE**

| Step | Status | Evidence |
|------|--------|----------|
| 1. User toggles `debug_mode = true` (setting wired) | OK | `lib.rs:420` `shortcut::change_debug_mode_setting` registered |
| 2. `DebugSettings.tsx` renders only when `debug_mode` true (parent gate) | OK | Component imports `SimulateUpdaterRestart` at line 7; mounts at line 36 |
| 3. Button calls Tauri command | OK | `SimulateUpdaterRestart.tsx:33` — `await commands.simulateUpdaterRestart()` |
| 4. Binding routes to backend | OK | `src/bindings.ts:558-559` |
| 5. Backend invokes `AppHandle::restart()` | OK | `commands/mod.rs:201-206` |
| 6. Restart path goes through Tauri updater machinery (same entrypoint as auto-update) | OK | Documented in `commands/mod.rs:189-200` doc-comment |

**Affected REQ-IDs:** SHUT-03.

---

## 3. Other Cross-Phase Issues Discovered

None. The following items were spot-checked and look consistent:

- **ICON-04 nuance** — `06-VERIFICATION.md:81` notes that the 1024×1024 source PNG lives in the external `dictus-brand` repo (not in `src-tauri/icons/`). REQUIREMENTS.md text says "under `src-tauri/icons/`", and the in-tree `icons/icon.png` is 512×512. This is a documented discrepancy already accepted by Phase 6 verification, not a new integration break.
- **TECH-04** — Recorded in REQUIREMENTS.md line 72 with `#[allow(clippy::too_many_arguments)]` suppression in `llm_client.rs:137`. The suppression is the deliberate path for keeping the clippy gate green per AUDIT-01; the refactor is deferred post-v1.2.
- **Patched-but-unused `tauri-runtime` warnings** — Emitted by Cargo as informational notices during `cargo clippy`. They are not clippy findings, do not fail `-D warnings`, and are unrelated to Phase 9 work.

---

## 4. Requirements Integration Map

| REQ-ID | Integration Path | Status | Issue |
|--------|------------------|--------|-------|
| BRAND-01 | `actions.rs:538` + `managers/history.rs:689` → `dictus-{ts}.wav` writes → tray + History UI display | WIRED | — |
| BRAND-02 | `portable.rs:29/97/112/161` → marker text shown in `DebugPaths.tsx` | WIRED | — |
| BRAND-03 | `commands::get_app_dir_path` (`lib.rs:464` registered) → `bindings.ts.getAppDirPath` → `DebugPaths.tsx:23` | WIRED | — |
| BRAND-04 | `.github/scripts/verify-sync.sh` lines 57-67 (3 BRAND assertions) | WIRED | — |
| ICON-01 | Linux PNGs 32-512 opaque (`identify [opaque]=True`); ICON-02a guard active | WIRED | — |
| ICON-02 | `icon.ico` 6 layers (16/24/32/48/64/256); guarded by verify-sync.sh:66-67 | WIRED | — |
| ICON-03 | `tauri.conf.json bundle.icon[]` lists 32x32, 128x128, 128x128@2x, 256x256, 512x512, .icns, .ico | WIRED | — |
| ICON-04 | Source PNG in external `dictus-brand` repo | WIRED (documented variance) | Source lives in sibling repo, not `src-tauri/icons/` — accepted in 06-VERIFICATION.md |
| SYNC-06 | `verify-sync.sh` lives at `.github/scripts/verify-sync.sh` (executable, 2876 bytes) | WIRED | — |
| SHUT-01 | `07-01-PLAN.md:161-200` `## Diagnosis` committed before Phase 7 fix lands | WIRED | — |
| SHUT-02 | `lib.rs:81` `flush_and_exit` helper → called at `lib.rs:301` (tray) + `lib.rs:675` (no-tray) | WIRED | — |
| SHUT-03 | `commands/mod.rs:201-206` `simulate_updater_restart` → registered `lib.rs:475` → `bindings.ts:558` → `SimulateUpdaterRestart.tsx` → mounted in `DebugSettings.tsx:36` | WIRED | — |
| PRIV-01 | `settings.rs:496-505` platform-aware default → `ProviderPicker.tsx:87/100` Local/Cloud tabs → `i18n tabs.local/tabs.cloud` in 20 locales | WIRED | — |
| PRIV-02 | `docs/PRIVACY.md` exists; linked from `README.md:55` and `PostProcessingSettings.tsx:590-594` (in-app "Learn more") | WIRED | — |
| PRIV-03 | `settings.rs:578` "Custom (local)" label + `settings.rs:482-484` `default_enable_cloud_providers = false` + onboarding presents local primary | WIRED | — |
| AUDIT-01 | `cargo clippy --all-targets -- -D warnings` exits 0; TECH-04 records deferred refactor | WIRED | — |
| AUDIT-02 | `07-VERIFICATION.md` `status: passed`, score 3/3 | WIRED | — |
| AUDIT-03 | `08-UAT.md` `status: passed` + 9 closure SHAs in `## Closure` section | WIRED | — |

**Requirements with no cross-phase wiring:** None. Every v1.2 requirement either (a) is itself a wiring requirement (BRAND-03, SHUT-02, SHUT-03, PRIV-01, PRIV-02) or (b) is a single-phase assertion that is independently re-checked by an automated gate that runs across the merged tree (verify-sync.sh, cargo clippy, settings unit tests, check:translations). No requirement is "stranded" in a single phase without an integration touchpoint.

---

## 5. Summary

**7/7 wiring concerns: WIRED. 5/5 E2E flows: COMPLETE. 0 cross-phase breaks discovered.**

The v1.2 milestone integrates cleanly across Phases 6-9. Phase 9's clippy sweep and verification backfill closed the three audit gaps without disturbing Phase 6's verify-sync.sh, Phase 7's `flush_and_exit` shutdown helper, or Phase 8's platform-aware default + Local/Cloud tabs. The merged tree builds, lints, tests, and translates without warnings; all SHUT/PRIV/AUDIT requirements have verifiable cross-phase paths.
